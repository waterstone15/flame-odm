(function() {
  var Config;

  Config = (function() {
    class Config {
      constructor(opts) {
        this.opts = opts;
      }

    };

    Config.prototype.type = 'Config';

    return Config;

  }).call(this);

  module.exports = Config;

}).call(this);
